# Контекст сборки nginx. Dockerfile копирует файлы поимённо, поэтому список
# короткий — он на случай появления COPY . . в будущем.

.git
.gitignore
*.md

# Секреты: .htpasswd монтируется томом в рантайме, в образ попадать не должен
.htpasswd
.env
