2026-01-30
*main

Что сделано:
- в раздел Наши продукты добавил ссылку на новый сайт Спорт и Походы в Сочи;
- обновил описание на первой странице сайта для показа в пошеренной ссылке в соцсетях;

2026-02-01
*main

Что сделано:
- добавил кастомный ./layouts/_default/baseof.html
- скопировал иконки соцсетей в /assets/social/
- вывел на сайте справа панель со ссылками на почту/телефон/телеграф

2026-02-07
*main

Что планируется:
[x] - вынести хранение префиксов для API из роутеров в main.py чтобы разгрузить nginx_gateway и не шарится по роутерам в поисках этих префиксов
[x] - запушить на гит
[]
[]
[]

Что сделано:
- унифицировал использование префиксов для API - /api/v2/
- протестировал роутеры формы и статистики - работает
- тестирование функционала - запуск скрипта в контейнере:
docker exec -i easysochi_contact_api python3 -< easysochi-backend/app/test/check_api.py

2026-02-08
*main

Что сделано:
- добавил в шаблон nginx фильтрацию логов от всякого мусора (favicon, статика, запросы от ботов);
- очистил файл от старых логов;
- пересобрал шаблон и перезапустил контейнер;
- теперь новый логи без мусора пишутся с 2026-02-08; 


2026-02-25
*main

Что сделано:
- добавил код яндекс метрики на сайт (layouts/partial/metrics.html)
- подключил в хедер layouts/_defauls/baseof.html     {{ partial "metrics.html" . }}
- добавил на сайт Вебмастер, подтвердил владение 


2026-02-28
*main

- В Dockerfile бэка прописал создание непривелигированного пользователя и запуск приложения от его имени. 
- сайту не нужен непривилигированный пользователь, потому что в его логике запуска прописан запуск от root и затем переключние на пользователя nginx с ограниченными правами
- перезапустил контейнер - сайт доступен
- клонировал репозиторий сайта с ГитХаб на новый сервер в /home/easysochi/Projects/easysochi_pro/EASYSOCHI.PRO
- актуализировал файл .env
- создал папку на диске HDD для данных /data/easysochi_pro/postgres_data
- прописал в docker compose монтировать туда том с БД
- поднял контейнер с базой на новом сервере
- скпировал данные с ноутбука на сервер (pg_dump)
- проверил подключение к БД - ОК

2026-03-03
*main

- создал на жестком диске раздел для медиа/логов/статистики
- выдал соответствующие права пользователю 1000 и 101 (nginx)
- добавил тома в docker compose
- создал папку nginx в корне проекта
- положил туда шаблон easysochi.conf.template
- скопировал файлы cloudflare_ids.conf/payments_ids.conf
- создал nginx/Dockerfile
- создал .dockerignore
- перенёс geo директиву payments_ids.conf на уровень http в nginx.conf
- добавил сервис nginx в docker-compose с зависимостями и healthcheck
- добавил общую сеть easysochipro_net всем сервисам
- убрал прямые ports у easysochi-site и contact_api — трафик только через nginx
- добавил healthcheck для easysochi-site и contact_api
- добавил эндпоинт /health в contact_api
- перевыпустил SSL сертификат для easysochi.pro (истёк 26 января)
- обновил верхний прокси — перезапустил для подхвата нового сертификата
- добавил сервис goaccess_pro в docker-compose
- создал .htpasswd с пользователем pro для защиты stats.easysochi.pro
- сайт и API работают, GoAccess открывается
- WebSocket для GoAccess не работает — верхний прокси не передаёт Upgrade заголовки - решено:

*** GoAccess по умолчанию подставляет внутренний порт WebSocket сервера в генерируемый HTML, игнорируя --ws-url. Чтобы браузер подключался через внешний прокси, нужно явно указать --ws-url=wss://домен:443/ws ***
- создан PR с фиксом WebSocket заголовков для верхнего прокси (ожидает merge Антона)


2026-03-20

Что сделано:
- составлена Политика в отношении обработки персональных данных (152-ФЗ)
- составлена Публичная оферта о добровольном пожертвовании (ст. 582 ГК РФ)
- добавлен дисклеймер под кнопкой «Перейти к оплате» со ссылками на оферту и политику
- настроен robotsNoIndex: true для страниц /agreement/ и /privacy-policy/

2026-09-10
*fix/htpasswd-leak

Что сделано:
- нашёл причину, почему .htpasswd вернулся в репозиторий после коммита fb88589:
  в .gitignore не было перевода строки в конце файла, и дописанный через >> паттерн
  склеился с предыдущим в нерабочий "progress.md.htpasswd"
- разделил склеенную строку обратно на progress.md и .htpasswd
- убрал .htpasswd из индекса через git rm --cached (файл на диске остался,
  nginx продолжает его монтировать)
- проверил: git check-ignore подтверждает, что .htpasswd теперь игнорируется
- добавил в README предупреждение, что создание .htpasswd обязательно до docker compose up:
  без файла Docker создаст на месте bind-mount директорию и nginx не поднимется

Что осталось сделать вручную на сервере:
[] - сменить пароль basic auth для stats.easysochi.pro: htpasswd -c .htpasswd pro
     старый хеш остаётся в истории коммитов публичного репозитория, ротация обязательна

2026-09-10
*feat/docker-hardening

Что сделано (по итогам ревью сборки docker):
- закрыл публикацию порта Postgres: было "${POSTGRES_PORT}:5432" на 0.0.0.0, стало
  "127.0.0.1:${POSTGRES_PORT}:5432". База больше не видна из интернета, доступ из
  SQL-клиента через SSH-туннель работает как раньше. Важно: docker пишет правила
  напрямую в цепочку DOCKER в iptables и обходит ufw, поэтому host_ip тут —
  единственная реальная защита
- починил определение реального IP клиента: cloudflare_ids.conf переименован в
  real_ip.conf, вместо real_ip_header CF-Connecting-IP теперь X-Forwarded-For с
  real_ip_recursive on, в доверенные сети добавлены верхний прокси и docker-подсети.
  До этого $remote_addr был адресом верхнего прокси, из-за чего geo $is_payment_service
  всегда возвращал 0 и вебхуки ЮKassa получали 403, а GoAccess считал всех посетителей
  одним IP
- убрал дублирующий include cloudflare_ids.conf из server-блока: файл и так
  подключается автоматически через include conf.d/*.conf на уровне http
- ограничил логи docker: якорь x-logging с max-size 10m / max-file 3 на все 5 сервисов
- добавил deploy/logrotate/easysochi_pro для ротации логов nginx на хосте,
  описал установку в README (шаг 5)
- GoAccess переведён на --persist/--restore с БД в отдельном томе, теперь ротация
  лога не стирает накопленную статистику
- миграции применяются автоматически: easysochi-backend/entrypoint.sh делает
  alembic upgrade head и затем exec uvicorn. В healthcheck contact_api добавлен
  start_period 40s, чтобы миграции успели пройти до первой проверки
- удалил из main.py startup-хук с create_all: он вызывался у Base из db_async.py, а все
  модели наследуются от Base из db.py, то есть metadata была пустая и хук ничего не делал
- добавил PYTHONUNBUFFERED=1 в Dockerfile бэкенда, иначе вывод alembic буферизуется
  и теряется, если контейнер падает на миграции

Требует проверки на сервере:
[] - доходят ли вебхуки ЮKassa после фикса real_ip:
     grep -i webhook /data/easysochi_pro/logs/easysochi.pro_access.log | tail -20
     Фикс исходит из того, что верхний прокси проставляет X-Forwarded-For. Если он его
     не ставит, $remote_addr не подменится и 403 останется — тогда чинить верхний прокси
[] - показывает ли GoAccess разные IP посетителей после перезапуска nginx

2026-09-10
*fix/robokassa-webhook-plaintext

Что сделано:
- вебхук Robokassa отвечал JSON {"status":"ok","detail":"OK123"}, хотя ResultURL по
  документации требует в теле ровно "OK<номер счёта>" открытым текстом. Строка уже
  формировалась в robokassa_service, но роутер оборачивал её в WebhookResponse.
  Robokassa такой ответ считает неуспехом и повторяет уведомление
- заменил response_model=WebhookResponse на response_class=PlainTextResponse,
  успешная обработка отдаёт OK<InvId> текстом
- неверная подпись и нехватка параметров теперь дают 400, а не 200: подделанное
  уведомление больше не выглядит принятым и отличимо в логах
- необработанное исключение отдаёт 500 — это сигнал Robokassa повторить позже,
  платёж при этом мог пройти
- удалил схему WebhookResponse, после правки она нигде не используется
- обновил тесты 3.1 и 3.2 в test_payment_api.py: проверяют текст ответа и код 400
  вместо разбора JSON

Проверить в личном кабинете Robokassa: перестали ли повторяться уведомления
по одному InvId.

2026-09-10
*chore/logging-hygiene

Что сделано:
- SQLAlchemy больше не пишет запросы в лог: db_async.py задавал
  sqlalchemy.engine уровень INFO, из-за чего в docker logs попадали email'ы,
  имена и SignatureValue платежей. Теперь WARNING по умолчанию, INFO включается
  переменной SQL_ECHO=1. Уровень задан явно, а не наследуется от корневого
  логгера: SQLAlchemy проверяет isEnabledFor(INFO), и при root=INFO запросы
  вернулись бы в лог
- убрал logging.basicConfig() из db_async.py: настройка корневого логгера как
  побочный эффект импорта модуля БД. Настройка перенесена в main.py, уровень
  задаётся переменной LOG_LEVEL
- побочный эффект прежней схемы: у root оставался уровень WARNING, поэтому все
  logger.info из наших модулей никуда не попадали. Теперь они видны
- добавил фильтр на uvicorn.access, отбрасывающий запросы /health: healthcheck
  ходит каждые 10 секунд, это ~8600 строк в сутки, которые вытесняли полезные
  записи из окна ротации docker-логов
- убрал print с персональными данными: form.py печатал тело ответа Telegram,
  а оно содержит эхо отправленного сообщения — имя, email и текст заявки
- убрал PAYMENT DEBUG из robokassa_service и логирование полного payload
  вебхука в donations.py (там Shp_email, Shp_name, SignatureValue)
- добавил LOG_LEVEL и SQL_ECHO в .env.example

2026-09-11
*fix/dockerignore-per-context

Что сделано:
- .dockerignore лежал в корне репозитория, а контексты сборки — ./easysochi-site,
  ./easysochi-backend и ./nginx. Docker читает этот файл из корня контекста, поэтому
  корневой не применялся ни к одной сборке
- создал по .dockerignore в каждом контексте, корневой удалил, чтобы не вводил
  в заблуждение
- главное для сайта: из контекста исключены public, resources и .hugo_build.lock.
  public всё равно пересоздаётся hugo --minify, а устаревший resources с машины
  сборки мог дать в образе не тот CSS/JS
- у бэкенда исключены __pycache__, .pytest_cache, виртуальные окружения и .env
- отдельно проверил, что не исключается лишнее: easysochi-site/nginx.conf нужен
  production-стадии, content/**/*.md — это содержимое сайта, app/test нужен
  в образе, потому что тесты гоняются внутри контейнера

Грабли, ради которых пришлось переписывать файлы:
- шаблоны в .dockerignore, в отличие от .gitignore, НЕ рекурсивны. "__pycache__"
  совпадает только с каталогом в корне контекста, для вложенных нужен "**/".
  Первый вариант файлов был бесполезен именно из-за этого
- шаблон "README.md" привязан к корню контекста и вложенные .md не задевает,
  поэтому content/*.md в безопасности

Проверка: написал скрипт, повторяющий логику сопоставления Docker, и прогнал его
по реальным спискам файлов всех трёх контекстов. Из 221 файла сайта исключается 11
(README, LICENSE, скрипты картинок, .github темы), из 37 файлов бэкенда — 1
(.gitignore), из 6 файлов nginx — ни одного.

2026-09-13
*docs/contributing

Что сделано:
- зафиксировал в CONTRIBUTING.md порядок работы в паре: постановка задачи из
  бэклога, ветка и PR, проверка на сервере, мерж либо доработка
- в разделе про PR закреплены обязательные пункты описания: что сделано,
  что проверено, что НЕ проверено и почему, что проверить на сервере и нужны ли
  изменения в .env
- в разделе про проверку описано переключение на ветку PR через
  git fetch + git switch, без создания локальной ветки: именно так на сервере
  появились коммиты мимо GitHub и расхождение с origin
- отдельно оговорено, что рабочая копия на сервере и есть прод, поэтому
  переключение на ветку PR — это выкладка непроверенного кода, и что откат
  делается через git switch main && docker compose up -d --build
- поправлена собственная рекомендация: строка "Built 0.0s" ненадёжна, она
  бывает нулевой и при реальной пересборке. Проверять дату образа через
  docker image inspect
- собран раздел "Грабли": нерекурсивные шаблоны .dockerignore, обход ufw
  докером, файл без перевода строки в конце, обязательный .htpasswd до первого
  up, кеш SSH-туннеля в DBeaver, test_payment_api.py как скрипт, а не pytest
- ссылка на CONTRIBUTING.md добавлена в README

Осталось от пункта про .env.example: ROBOKASSA_* добавлены, но YOOKASSA_* в файле
остались мёртвыми — уедут вместе с app/routers/_donations.py.

2026-09-13
*feat/payment-provider-switch

Что сделано:
- платёжная система стала переключаемой через PAYMENT_PROVIDER в .env,
  допустимые значения robokassa и yookassa. Одновременно активна ровно одна
- добавлена фабрика get_payment_service() в app/services/__init__.py. При
  неизвестном значении переменной она бросает ValueError на импорте роутера,
  то есть контейнер не поднимется — это лучше, чем молча принимать платежи
  не в ту систему
- логика ЮKassa перенесена из app/routers/_donations.py в полноценный сервис
  app/services/yookassa_service.py, старый роутер удалён. Он не был подключён
  к приложению с момента перехода на Robokassa
- в абстракцию PaymentService добавлен метод parse_webhook: Robokassa шлёт
  form-data, ЮKassa - JSON, и роутер больше не знает об этой разнице
- в сигнатуру create_payment у абстракции добавлен user_id. RobokassaService
  принимал его и раньше, а интерфейс о нём не знал: ABC проверяет только
  наличие метода, но не сигнатуру, поэтому расхождение никак не проявлялось
- синхронный вызов YookassaPayment.create уведён в asyncio.to_thread, иначе
  блокирующий HTTP-запрос тормозит весь воркер

База данных (миграция a1b2c3d4e5f6):
- добавлена колонка payments.provider, существующие записи забэкфилены
  значением robokassa
- yk_payment_id переименована в provider_payment_id: имя осталось от ЮKassa,
  а колонка давно хранит InvId от Robokassa
- уникальность перенесена с одной колонки на пару (provider,
  provider_payment_id): InvId у Robokassa это 32-битное число, id у ЮKassa
  это UUID, глобальная уникальность одного идентификатора смысла не имеет
- поиск платежа в вебхуке теперь идёт по паре, а не по одному идентификатору

Попутно исправлен баг:
- RobokassaService создавал платёж с metadata={"email": ..., "name": ...},
  хотя в модели колонка называется extradata. TypeError не возникал, потому
  что metadata существует у любого declarative-класса SQLAlchemy как объект
  MetaData, и присваивание просто перекрывало его на экземпляре. Email и имя
  при этом молча не сохранялись
- вторая половина того же бага: /donations/stats читал payment.metadata.get()
  для анонимных донатов. У записи, загруженной из БД, это объект MetaData,
  у которого нет метода get, то есть первый успешный анонимный донат в
  последней десятке уронил бы эндпоинт статистики в 500. Сейчас читается
  extradata

ВАЖНО при откате: миграция переименовывает колонку, поэтому просто вернуться
на main недостаточно - старый код будет обращаться к yk_payment_id. Сначала
docker exec easysochi_contact_api alembic downgrade -1, потом переключение
ветки и пересборка.

2026-09-13
*feat/build-script-and-api-checks

Что сделано:
- тест окончательно стал скриптом: test_payment_api.py переименован в
  check_payment_api.py, функции test_api и test_webhook_manually - в
  run_checks и manual_webhook_check. Теперь pytest его не подбирает и не
  показывает FAILED при нулевом коде возврата
- pytest не нужен вообще: скрипту хватает httpx, а это боевая зависимость
  приложения. Никакого requirements-dev.txt заводить не пришлось, тест
  работает внутри продового контейнера без единой лишней библиотеки
- в шапку скрипта дописано подробное описание: что именно проверяется по
  разделам, чего скрипт НЕ проверяет (geo-фильтр и real_ip не задействованы,
  потому что запросы идут мимо nginx), как запускать, что означают коды
  возврата и какие переменные окружения нужны
- информативный вывод при ошибке: каждая упавшая проверка складывается в
  список FAILURES и печатается отдельным блоком в конце, с телом ответа
  сервера. Раньше пришлось бы выискивать красные строки в простыне вывода
- добавлена преднастроечная проверка /health: если API не поднялся, скрипт
  говорит об этом одной понятной строкой и выходит, а не сыплет двумя
  десятками одинаковых ConnectError
- app/test_db_async.py переехал в app/test/check_db_async.py, функция test_db
  переименована в check_db: файл лежал прямо в app/, назывался test_*.py и
  содержал test_db(), то есть pytest подобрал бы его по имени, а он вставляет
  настоящую запись в contact_form боевой базы
- класс TestColors переименован в Colors: pytest по умолчанию собирает и
  классы с именами Test*
- удалён check_api.py: он проверял статистику и валидацию формы, то есть
  подмножество того, что делает check_payment_api.py. Точка входа для
  проверок теперь одна
- образы получили имена и теги: easysochi/site, easysochi/contact-api,
  easysochi/nginx с тегом ${TAG:-latest}
- добавлен scripts/build.sh: предпроверки (демон docker, .env, .htpasswd),
  сборка с тегом по хешу коммита, пометка :latest, запуск, ожидание healthy
  и прогон проверок API. При неудаче сам печатает docker compose ps и хвост
  логов только тех контейнеров, что не поднялись
- тег получает суффикс -dirty, если в рабочем дереве есть незакоммиченные
  правки: образ из такого дерева не воспроизводится из коммита, и об этом
  честнее сказать сразу
- README и CONTRIBUTING приведены в соответствие: шаг сборки в протоколе
  теперь одна команда ./scripts/build.sh, описан откат по тегу и откат
  миграции

Откат на предыдущую сборку без пересборки, если образ ещё на диске:
TAG=<хеш> docker compose up -d
Список доступных тегов: docker images easysochi/contact-api

2026-09-13
*refactor/deploy-layout

Что сделано (воспроизведён паттерн из RWP_gateway):
- Dockerfile каждого сервиса переехал в его собственный каталог docker/:
  easysochi-site/docker/, easysochi-backend/docker/, nginx/docker/
- вся конфигурация развёртывания собрана в deploy/: docker-compose.yml,
  build.sh, .env.example и уже лежавший там logrotate/
- контексты сборки в compose стали ../<сервис>, а dockerfile: docker/Dockerfile
- .dockerignore остались на месте, в корне каждого сервиса: docker читает их
  из корня КОНТЕКСТА, а контекст по-прежнему каталог сервиса, не docker/

Главная тонкость переезда:
- compose выводит имя проекта из каталога, где лежит compose-файл. После
  переезда в deploy/ проект назывался бы deploy, и сеть с томами получили бы
  имена deploy_* вместо easysochipro_* — то есть были бы пересозданы, а
  контейнеры отцепились бы от существующих данных
- поэтому в compose-файл добавлена строка name: easysochipro. Сверил вывод
  docker compose config до и после переезда: секции networks и volumes
  совпадают посимвольно
- этот же приём использован в RWP_gateway: там первой строкой name: es-wake

Что НЕ стал переносить:
- в RWP_gateway у каждого сервиса есть docker/build-image.sh. Там они нужны,
  потому что образы собираются отдельно от compose: сначала Gradle делает jar,
  и в compose у сервисов нет секции build, только image. У нас compose собирает
  сам, с правильным контекстом и тегом, поэтому три почти одинаковых скрипта
  дублировали бы то, что уже делает docker compose build

ВАЖНО при обновлении сервера: .env и .htpasswd теперь ожидаются в deploy/,
рядом с compose-файлом. Разово перенести:
  mv .env deploy/.env
  mv .htpasswd deploy/.htpasswd
Если забыть, deploy/build.sh остановится на предпроверках и покажет обе
команды: он специально смотрит, не остался ли файл в корне.

Бэклог (правки не относятся к переезду, положены сюда, чтобы не ловить конфликт
по progress.md с двумя открытыми PR):
- отмечен выполненным пункт про print() с персональными данными в form.py:
  закрыт в PR #7, в файле не осталось ни одного print, в лог уходят только id
  записи и код ответа Telegram
- добавлен пункт среднего приоритета про перевод /form/ на pydantic-схему

После мержа PR #11 GitHub не перенацелил этот PR на main, а закрыл его: базовая
ветка была удалена, и автоматика сработала не так, как в документации. Ветка
refactor/deploy-layout уцелела, коммиты на месте. Восстановлено возвратом базовой
ветки на минуту: push ветки на коммит deeff5f, который уже в main, затем
gh pr reopen 12, gh pr edit 12 --base main, удаление временной ветки. Номер PR и
обсуждение сохранились. В CONTRIBUTING переписан раздел про стековые PR: теперь
там сказано перенацеливать зависимый PR на main ВРУЧНУЮ до мержа базового, а не
надеяться на автоматику, и приведена процедура восстановления.

2026-09-13
*fix/scope-container-secrets

Что сделано:
- env_file: .env убран у всех сервисов, вместо него явный environment с
  перечислением переменных поимённо. Раньше весь файл целиком уезжал в nginx,
  в базу и в API
- nginx получал TELEGRAM_TOKEN, POSTGRES_PASSWORD и ключи платёжных систем,
  хотя ему нужны ровно две переменные для envsubst: DOMAIN_PRO и PRO_STATS.
  Теперь только они
- db_easysochi получал ключи ЮKassa и Robokassa. Теперь только POSTGRES_DB,
  POSTGRES_USER и POSTGRES_PASSWORD, которые и так были прописаны явно ниже:
  env_file там просто дублировал environment
- contact_api получает то, что реально читает: DATABASE_URL, DATABASE_URL_SYNC,
  переменные платёжной системы, TELEGRAM_TOKEN, CHAT_ID, DOMAIN_URL, LOG_LEVEL
  и SQL_ECHO. POSTGRES_PASSWORD ему не нужен - в базу он ходит по DATABASE_URL

Подводный камень, который пришлось обойти:
- при env_file переменная, отсутствующая в файле, просто не попадает в
  контейнер, и умолчание из кода работает. При явном environment она приезжает
  ПУСТОЙ СТРОКОЙ и умолчание перекрывает. Для LOG_LEVEL это падение при старте:
  logging.basicConfig(level="") бросает ValueError: Unknown level
- поэтому у необязательных переменных проставлены умолчания прямо в compose
  через ${VAR:-default}, а в main.py getenv("LOG_LEVEL", "INFO") заменён на
  getenv("LOG_LEVEL") or "INFO" - на случай запуска мимо compose

Ещё:
- в deploy/build.sh добавлена проверка обязательных переменных в deploy/.env.
  Compose на отсутствие переменной только предупреждает и подставляет пустую
  строку: база поднялась бы без пароля, nginx получил бы пустой server_name.
  Проверка ловит и отсутствующую переменную, и заданную пустой
- в deploy/.env.example исправлен DOMAIN_URL: было записано как DOMAIN_URL = ""
  с пробелами и пустым значением, хотя бэкенд эту переменную читает и использует
  как return_url после оплаты

2026-09-13
*feat/form-topic-and-schema

Что сделано:
- форма обратной связи получила тему обращения, выбор способа связи
  (email/телефон/telegram) и источник — страницу, с которой отправлена
- /form/ переведён на pydantic-схему ContactFormCreate. Раньше роутер читал
  сырой JSON и проверял поля вручную: формат email не проверялся вовсе, а
  сообщение длиннее String(2000) упиралось в колонку и возвращало 500 "DB error"
  вместо понятного отказа. Теперь невалидный ввод даёт 422 с указанием поля
- добавлен GET /api/v2/form/topics — отдаёт список тем из enum ContactTopic
- форма вынесена из content/contact/_index.md в шорткод
  layouts/shortcodes/contact_form.html. Страница теперь содержит одну строку
  {{< contact_form >}}, а форму можно поставить куда угодно — пригодится для
  запроса обратного звонка на главной

Решение по справочнику тем:
- на easy-sochi.ru тема тянется из таблицы categories, которая ссылается на
  product_types, и ради формы там заведён фиктивный тип продукта "Форма"
- у нас нет ни products, ни product_types, поэтому две таблицы ради списка из
  шести тем не заводил. Тема — enum в схеме, в базе строка. Единственный
  источник правды это ContactTopic, фронт берёт список эндпоинтом
- отдельная таблица понадобится, только если темы должны меняться без деплоя

Чего намеренно не перенёс с easy-sochi.ru:
- запрос к https://ipapi.co/json/ для определения кода страны: он уходит при
  каждой загрузке страницы и передаёт IP посетителя третьей стороне, что не
  описано в нашей политике по 152-ФЗ. Плюс он стоит под await перед
  инициализацией формы — если сервис недоступен, форма не работает вовсе.
  Код страны зафиксирован как +7
- семь print() в роутере, включая print тела ответа Telegram: оно содержит
  эхо заявки, то есть имя, контакт и текст в логах контейнера. Ровно это мы
  вычистили у себя в PR #7
- response_model, возвращающий клиенту всю запись с контактом. Отдаём только
  идентификатор
- alert() с JSON.stringify(detail): вместо него человеческая формулировка
  причины из detail[0].msg и блок статуса прямо в форме, подробности в консоль

База (миграция b2c3d4e5f6a7):
- добавлены contact_type (бэкфилл 'email'), topic (бэкфилл 'Другое') и source
- email переименована в contact_value: колонка давно перестала быть про почту
- source оставлен nullable: у старых записей источник неизвестен

Доработка после проверки на сервере:
- уведомление в Telegram уехало в BackgroundTasks: ответ клиенту отдаётся сразу
  после коммита в базу. Раньше отправка шла внутри обработчика, Telegram с
  сервера недоступен, и запрос висел все 10 секунд таймаута. Посетитель видел,
  что ничего не происходит, жал ещё раз — в базе появлялись дубли (id 19 и 20,
  затем 21, 22, 23)
- на фронте кнопка блокируется на время запроса и показывает "Отправляем…".
  Быстрого ответа мало: сеть у посетителя может быть медленной
- сообщение об успехе убирается через 8 секунд, сообщение об ошибке остаётся:
  его нужно успеть прочитать
- иконки на кнопках не показывались из-за специфичности CSS: правило
  #contact-form button в contact.css (1-0-1) перебивало класс
  .contact-form-tab-btn (0-1-0) и навязывало кнопкам синий фон, свой padding и
  border: none. Селекторы переписаны с префиксом #contact-form, под иконкой
  добавлена подпись — кнопка остаётся понятной, даже если картинка не
  загрузится
- логирование ошибок Telegram: было logger.warning("...: %s", exc), а у
  таймаутов httpx пустое строковое представление, поэтому в логах оставалось
  "Telegram notification failed:" без причины. Теперь %r, а при ответе с кодом
  не 200 пишется ещё и description из тела ошибки
- то же исправление в NotificationService: уведомления о платежах молча
  терялись по той же причине

Проверки:
- прогнал схему на 16 случаях локально, pydantic из anaconda: корректные и
  битые email, короткий телефон, telegram без @, границы длины имени,
  сообщения и source, неизвестные тема и способ связи
- в check_payment_api.py раздел 4 переписан: добавлена проверка /topics,
  а ожидаемый код невалидного ввода изменён с 400 на 422 — иначе тест 4.1
  упал бы после перехода на схему. Добавлены проверки 4.3 и 4.4 на
  несоответствие контакта способу связи и на тему вне списка

2026-09-13
*refactor/hugo-build-image

Что сделано:
- сборка сайта переведена с ubuntu:22.04 + wget на готовый образ
  hugomods/hugo:base-0.146.0. Раньше каждая сборка тянула с GitHub 40+ МБ
  архива Hugo без сверки контрольной суммы и ломалась бы, будь GitHub
  недоступен. Плюс отдельный stage на ubuntu существовал только ради wget
- заодно "as builder" заменено на "AS builder" — BuildKit ругался FromAsCasing

Про выбор тега:
- в бэклоге я записала hugomods/hugo:exts-0.146.0, но ТАКОГО ТЕГА НЕ
  СУЩЕСТВУЕТ. Проверил по Docker Hub: есть 0.146.0, base-, reg-, std-,
  std-exts-, reg-exts- и другие
- у hugomods редакция задаётся префиксом: теги БЕЗ std это extended,
  с std — обычная редакция. exts добавляет PostCSS, Asciidoc и Pandoc,
  которые нам не нужны
- взял base-0.146.0: минимальный образ ровно с extended-Hugo, без Node, Go и
  Git. 44 МБ, есть под amd64 и arm64
- extended сохранён намеренно, хотя SCSS в проекте нет: прежний Dockerfile
  качал hugo_extended, а PaperMod проверяет hugo.IsExtended и добавляет webp
  в список обрабатываемых форматов обложек. Менять редакцию заодно с
  переездом на готовый образ — лишний риск, это отдельное решение
- git в образе не нужен: модули Hugo не используются, enableGitInfo не задан,
  тема PaperMod лежит каталогом в themes/

================================План на обновление EASYSOCHI.PRO======================
*******************Приоритет: Высокий*****************************************
[] - уведомления в Telegram не доходят с сервера ВООБЩЕ: в логах от 2026-09-13 все
     попытки отправки завершились ошибкой. Заявки сохраняются в базу, но о них никто
     не узнаёт. Причина пока неизвестна — раньше текст ошибки был пустым, теперь
     логируется через %r плюс description из тела ответа Telegram, так что после
     ближайшего деплоя причина будет видна в docker logs. Проверить в первую очередь
     доступность api.telegram.org с сервера и правильность TELEGRAM_TOKEN/CHAT_ID
[x] - политику обработки данных изобрести и ссылку прикрепить при отправке любой формы
[] - продумать создание пользователя в таблице users после отправки формы. Может быть крон запускать каждый день и проверять записи из формы на наличие в таблице users и создавать, если нет, и привязывать юзера к сообщения, чтобы историчность была
[] - 
[] - 
[x] - добавить в форму обратной связи тему обращения (по аналогии с easy-sochi.ru)
[] - добавить на главную форму Запрос обратного звонка (взять из easy-sochi.ru)
[x] - добавить яндекс.метрику и вебмастер


--- инфраструктура, по итогам ревью docker 2026-09-10 ---
[x] - секреты раздаются контейнерам, которым не нужны: env_file: .env стоит у nginx и
     db_easysochi. Nginx получает TELEGRAM_TOKEN/POSTGRES_PASSWORD/YOOKASSA_SECRET_KEY,
     хотя ему нужны только DOMAIN_PRO и PRO_STATS. Заменить на явный environment
[x] - routers/form.py пишет через print() имя, email и текст сообщения в логи контейнера.
     Это персональные данные в открытом виде — после принятия политики 152-ФЗ так нельзя.
     Убрать print, оставить logger с обезличенными сообщениями

******************Приоритет: Средний*******************************************
[] - уведомление об успешном платеже в robokassa_service отправляется прямо в
     обработчике вебхука, до commit и до ответа. При недоступном Telegram это
     задерживает ответ на 5 секунд таймаута, а Robokassa при задержке считает
     уведомление недоставленным и шлёт повтор. Увести в BackgroundTasks, как уже
     сделано в /form/
[] - 
[] - 
[] - 


--- инфраструктура, по итогам ревью docker 2026-09-10 ---
[x] - .dockerignore не применяется ни к одной сборке: он в корне, а контексты сборки —
     ./easysochi-site, ./easysochi-backend, ./nginx. Docker читает .dockerignore из корня
     контекста. Для сайта критично: в образ утягиваются public/, resources/ и
     .hugo_build.lock с машины сборки, устаревший кеш resources/ может испортить сборку
[] - goaccess_pro стартует раньше, чем nginx создаст лог, и уходит в цикл рестартов.
     Добавить depends_on на nginx
[x] - Hugo качается wget-ом при каждой сборке: 40+ МБ с GitHub, без сверки контрольной
     суммы, плюс целый stage на ubuntu:22.04 ради wget. Перейти на готовый образ
     hugomods/hugo:exts-0.146.0 — быстрее, меньше, версия зафиксирована
[] - тома с driver_opts type=none/o=bind — это bind-mount, переодетый в volume. Ловушка:
     при смене device: докер не пересоздаёт том, пока не сделать docker volume rm, и
     изменения молча не применяются. Заменить на обычные bind-mount

[x] - .env.example не соответствует коду после перехода на Robokassa: в нём остались
     YOOKASSA_*, а ROBOKASSA_SHOP_ID / PASSWORD_1 / PASSWORD_2 / TEST_MODE отсутствуют.
     На новом сервере платежи молча не заведутся — settings подставит пустые строки
[x] - app/routers/_donations.py — старый роутер ЮKassa, нигде не импортируется.
     Удалить или перенести в отдельную ветку, чтобы не путал при поиске по коду

[x] - /form/ не использует pydantic-схему: читает сырой JSON через request.json() и
     проверяет поля вручную, поэтому нет проверки ни формата email, ни длины полей.
     Любая строка проходит как email, а сообщение длиннее 2000 символов упирается в
     String(2000) в модели, вылетает SQLAlchemyError и клиент получает 500 "DB error"
     вместо понятного 400. Перевести на схему по образцу DonationRequest: EmailStr и
     max_length. Учесть, что EmailStr требует пакет email-validator (pydantic[email])

********************Приоритет: Низкий*******************************************
[] - SERVER_IP в deploy/.env.example не читается ни кодом, ни compose, ни nginx —
     остался от прежней конфигурации. Убрать, чтобы не создавал видимость нужной
     настройки
[] - 
[] - 


--- инфраструктура, по итогам ревью docker 2026-09-10 ---
[] - apk add --no-cache gettext в nginx/Dockerfile лишний: envsubst есть в образе всегда, им пользуется сам entrypoint 20-envsubst-on-templates.sh
[] - из базового образа остаётся /etc/nginx/conf.d/default.conf и подключается через include conf.d/*.conf — мёртвый server-блок на 80. Удалить в Dockerfile
[x] - в easysochi-site/Dockerfile "as builder" строчными — BuildKit ругается FromAsCasing
[] - user: "1000:1000" в compose дублирует USER easysochipro из Dockerfile,
     два источника правды для одного uid
[x] - образы без явных имён и тегов, откатиться на предыдущую сборку нельзя.
     Задать image: easysochi/<сервис>:${TAG:-latest}
[] - docker compose падает без .env с "env file not found" — дописать в README, что это
     обязательный шаг до up, рядом с предупреждением про .htpasswd
[] - restart вразнобой: always у db и goaccess, unless-stopped у остальных
